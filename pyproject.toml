[project]
name = "home-intrusion-alert-system"
version = "0.1.0"
requires-python = ">=3.11,<3.12"

dependencies = [
    "streamlit",
    "numpy",
    "scipy",
    "scikit-fuzzy",
    "networkx",
    "matplotlib"
]
